{% macro Slider(
  class,
  gap = "1.09rem",
  columnSize = "max(300px, 83vw)",
  attributes
) %}
  <div
    {{ attributes }}
    class="Slider {{ class }}"
    style="
      --gap: {{ gap }};
      --columnSize: {{ columnSize }};
    "
  >
    {{ caller() }}
  </div>
{% endmacro %}

{% macro Slider(
  class,
  gap = "0.72rem",
  columnSize = "max(300px, 90vw)",
  attributes
) %}
  <div
    {{ attributes }}
    class="Slider {{ class }}"
    style="
      --gap: {{ gap }};
      --columnSize: {{ columnSize }};
    "
  >
    {{ caller() }}
  </div>
{% endmacro %}
